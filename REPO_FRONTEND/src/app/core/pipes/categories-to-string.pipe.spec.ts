import { CategoriesToStringPipe } from './categories-to-string.pipe';

describe('CategoriesToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriesToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
