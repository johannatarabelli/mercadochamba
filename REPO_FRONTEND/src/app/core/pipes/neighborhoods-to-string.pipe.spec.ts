import { NeighborhoodsToStringPipe } from './neighborhoods-to-string.pipe';

describe('NeighborhoodsToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new NeighborhoodsToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
